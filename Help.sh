echo Here are the commands
echo Exit (Exit.sh) exists the current SuperTerm instance
echo Lmao (Lmao.sh) a command you can use if you're bored
echo Sheep (Sheep.sh) I heard a sheep :0
echo SuperTerm (SuperTerm.sh) this starts SuperTerm.
echo Ver (Ver.sh) displays version of superterm being ran.
echo Install (Install.sh) puts the files of SuperTerm in /usr/bin/ so that they can be ran from anywhere in your filesystem
